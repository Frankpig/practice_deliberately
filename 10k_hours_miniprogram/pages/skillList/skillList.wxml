<!-- 技能列表页面 - 重制版 -->
<view class="page-container" style="background-color: {{darkMode ? '#344e41' : '#dad7cd'}}; transition: background-color 0.3s ease;">
  <!-- 页面头部 -->
  <view class="page-header">
    <h1 class="page-title" style="color: {{darkMode ? '#dad7cd' : '#344e41'}};">我的技能</h1>
    <view class="skill-count" style="background-color: {{darkMode ? '#3a5a40' : '#ffffff'}}; color: {{darkMode ? '#a3b18a' : '#588157'}};">{{skills.length}} 项</view>
  </view>
  
  <!-- 空状态 -->
  <view wx:if="{{skills.length === 0}}" class="empty-state">
    <view class="empty-icon">🎯</view>
    <h3 class="empty-title" style="color: {{darkMode ? '#dad7cd' : '#344e41'}};">暂无技能</h3>
    <p class="empty-description" style="color: {{darkMode ? '#a3b18a' : '#588157'}};">点击底部的「+」按钮添加新技能，开始你的练习之旅</p>
  </view>
  
  <!-- 技能列表 -->
  <view wx:else class="skill-list">
    <block wx:for="{{skills}}" wx:key="id">
      <view class="skill-card" data-skill-id="{{item.id}}" data-skill-name="{{item.name}}" style="background-color: {{darkMode ? '#3a5a40' : '#ffffff'}}; border-color: {{darkMode ? '#588157' : '#a3b18a'}}; color: {{darkMode ? '#dad7cd' : '#344e41'}};">
        <!-- 技能卡片头部 -->
        <view class="skill-header">
          <h3 class="skill-name" bindtap="onSkillTap" data-skill-id="{{item.id}}" data-skill-name="{{item.name}}" style="color: {{darkMode ? '#dad7cd' : '#344e41'}};">{{item.name}}</h3>
          <view class="skill-actions">
            <view class="skill-stage" style="background-color: {{darkMode ? '#a3b18a' : '#3a5a40'}}; color: {{darkMode ? '#344e41' : '#ffffff'}};">{{item.stage}}</view>
            <view class="delete-btn" bindtap="onDeleteSkill" data-skill-id="{{item.id}}" data-skill-name="{{item.name}}" style="background-color: {{darkMode ? '#3a5a40' : '#ffffff'}}; border-color: {{darkMode ? '#588157' : '#a3b18a'}};">
              <text class="delete-icon" style="color: {{darkMode ? '#a3b18a' : '#588157'}};">×</text>
            </view>
          </view>
        </view>
        
        <!-- 进度信息 -->
        <view class="progress-section">
          <!-- 进度条 -->
          <view class="progress-wrapper">
            <view class="progress-bar-container" style="background-color: {{darkMode ? '#344e41' : '#dad7cd'}}; border-color: {{darkMode ? '#588157' : '#a3b18a'}};">
              <view class="progress-bar" style="width: {{item.progressPercentage}}%; background: linear-gradient(90deg, #a3b18a, #588157);">
                <view class="progress-glow"></view>
              </view>
            </view>
            <view class="progress-percentage" style="color: {{darkMode ? '#a3b18a' : '#3a5a40'}};">{{item.progressPercentage}}%</view>
          </view>
          
          <!-- 小时数信息 -->
          <view class="hours-info">
            <text class="current-hours" style="color: {{darkMode ? '#a3b18a' : '#3a5a40'}};">{{item.hoursPracticed}} 小时</text>
            <text class="separator" style="color: {{darkMode ? '#588157' : '#588157'}};">/</text>
            <text class="target-hours" style="color: {{darkMode ? '#588157' : '#588157'}};">{{item.targetHours}} 小时目标</text>
          </view>
        </view>
        
        <!-- 预计完成时间 -->
        <view class="estimated-time" style="border-top-color: {{darkMode ? '#588157' : '#a3b18a'}};">
          <text class="time-label" style="color: {{darkMode ? '#588157' : '#588157'}};">预计需要:</text>
          <text class="time-value" style="color: {{darkMode ? '#dad7cd' : '#344e41'}};">{{calculateRemainingTime(item.hoursPracticed, item.targetHours)}}</text>
        </view>
      </view>
    </block>
  </view>
  

</view>